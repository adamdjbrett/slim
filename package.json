{
  "name": "slim-11ty-port",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf _site && eleventy --config=eleventy.config.js && pagefind --site _site --glob \"**/*.html\"",
    "start": "eleventy --serve --config=eleventy.config.js --incremental"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.5",
    "@11ty/eleventy-plugin-rss": "^2.0.2",
    "luxon": "^3.5.0",
    "pagefind": "^1.3.0"
  }
}
